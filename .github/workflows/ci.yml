name: CI

on:
  pull_request:
  push:
    branches:
      - main
      - feature/aiquota-mvp

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Type-check (src project references)
        run: npx tsc -b

      - name: Type-check (test/**)
        run: npx tsc --noEmit -p test/tsconfig.json

      - name: Run tests
        run: npx vitest run
        env:
          CI: true
